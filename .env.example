# ─────────────────────────────────────────────
#  Copiar este archivo como .env y completarlo
#    cp .env.example .env
# ─────────────────────────────────────────────

# Dominio público de la app (debe coincidir con el DNS configurado)
DOMAIN=eshop.digitaldiscos.com.ar

# Puerto interno en el que escucha Gunicorn (dentro del contenedor)
APP_PORT=5000

# Clave secreta de Flask — CAMBIAR en producción por un valor aleatorio largo
# Generar con: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-to-a-long-random-string
